# 额外任务要求：{{任务名称}}

**目标**：
> 简述此任务的目的（例如：积累问答库、提取待办事项、生成摘要）。

**执行方式**：
除了标准的校对转写文件外，需要在 `5-最终输出/` 目录下额外生成文件。

1.  **文件命名**：`{{输出文件前缀}}-$topic.md` (例如 `问题和回答-模型能力对比.md`)
2.  **内容结构**：
    *   **字段1**：说明...
    *   **字段2**：说明...
3.  **写入策略**：
    *   **仅追加 (Append Only)**：避免覆盖已有信息。
    *   **时机**：在发现相关内容或话题切换后提取。

**关注点**：
*   关注点 1
*   关注点 2
