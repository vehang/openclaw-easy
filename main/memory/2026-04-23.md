# 2026-04-23 工作日志

## 模型配置

- 已添加 `glm-5-turbo` 模型，配置与 `glm-5.1` 相同（同 apiKey、baseUrl）
- 当前可用模型：`glm-5.1`、`glm-5-turbo`、`MiniMax-M2.7`

## 待推进事项（来自 4/18-4/19 讨论）

### 自动化开发工作流（用户核心需求）
用户想用飞书多机器人搭建虚拟开发团队，实现自动化代码开发循环：

**完整链路**：
```
需求 → PRD → 产品设计 → UI设计 → 技术方案 → 任务拆解 → 开发 → 测试 → 交付
```

**关键决策 - "可运行原型即设计稿"**：
- 用户认可的方向：设计稿 = 可运行的 HTML/CSS/JS 原型页面
- 旧项目场景：先分析旧项目代码提取设计系统，再用同风格做新功能原型
- 原型确认后 → 开发基于原型代码实现 → 测试对比原型截图 + 设计规范验证
- 用户还没回答的两个问题：
  1. UI设计阶段输出形式确认（倾向视觉设计稿）
  2. 先搭 skill 框架还是拿具体项目试跑

**用户强调**：
- 不急着写代码，前期讨论清楚再动手
- 旧项目上加新功能，设计必须跟旧项目一致
- 每阶段产出文档作为下游标准，测试必须有参考依据

## Dreaming 状态

- memory-core 插件已启用，但 cron 未配置
- `data/cron/` 目录为空，Dreaming 未实际运行
